#include"main.h"
/**
 * _abs - compute the absolute value of int
 *
 * @i the integre to compute
 *
 * Return: i
 */
int _abs(int i)
{

	if (i < 0)
	{
		i = -1 * i;
		return (i);
	}
	else
	{
		return (i);
	}
}
